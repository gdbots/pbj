<?php
declare(strict_types=1);

namespace {{ enum_to_native_namespace(enum) }};

enum {{ enum_to_class_name(enum) }}: {{ enum.type }}
{
{% for key, value in enum.values %}
    case {{ key|upper }} = {% if enum.type == 'int' %}{{ value }}{% else %}'{{ value }}'{% endif %};
{% endfor %}
}
