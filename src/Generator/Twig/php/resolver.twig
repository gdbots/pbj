<?php
declare(strict_types=1);

/**
 * DO NOT EDIT THIS FILE as it will be overwritten by the Pbj compiler.
 * @link https://github.com/gdbots/pbjc-php
 *
 * Registers all of the messages with the MessageResolver.
 *
 * @link {{ compile_options.domain }}/
 */

use Gdbots\Pbj\MessageResolver;

MessageResolver::setDefaultVendor('{{ compile_options.getDefaultVendor() }}');
MessageResolver::setManifestDir(__DIR__.'/manifests');
MessageResolver::register([
{% for curie, schema in manifests['messages'] %}
    '{{ curie }}' => '{{ schema_to_native_class_path(schema) }}',
{% endfor %}
]);
