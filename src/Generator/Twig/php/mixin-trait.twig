<?php
declare(strict_types=1);

namespace {{ schema_to_native_namespace(mixin) }};

{{ imports }}

/**
 * @method static Schema schema
 * @method mixed fget($fieldName, $default = null)
 */
trait {{ schema_to_class_name(mixin, true) }}Trait
{
{{ indent_string(methods, 4) }}
}
