{
  "schema_id": "{{ schema.id }}",
  "fields": {
    {% import 'json/fields.json.twig' as fields %}
    {% for field in schema.fields %}
      {{ attribute(fields, class(field.type), [field]) }}{% if not loop.last %},{% endif %}
    {% endfor %}
  }
}
