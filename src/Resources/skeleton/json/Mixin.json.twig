{
  "schema_id": "{{ schema.id }}",
  "fields": {
    {% import 'json/macros.json.twig' as macros %}
    {%- for field in schema.fields %}
      {{- macros.draw_field(field) }}{%- if not loop.last %},{% endif %}

    {% endfor -%}
  }
}
