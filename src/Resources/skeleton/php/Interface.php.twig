<?php

namespace {{ schema.languageKey('php', 'namespace') }};

{% block use_statements %}
{% if hasOtherMajorRev(schema) and schema.extends %}
use {{ schema.extends.languageKey('php', 'namespace') }}\{{ getClassName(schema.extends, true) }};
{% endif %}
{% endblock use_statements %}

{% block class_definition %}
interface {{ getClassName(schema, true) }} extends {% if hasOtherMajorRev(schema) and schema.extends %}{{ getClassName(schema.extends, true) }}{% else %}{{ getClassName(schema) }}{% endif %}

{% endblock class_definition %}
{
{% block class_body %}
{% endblock class_body %}
}
